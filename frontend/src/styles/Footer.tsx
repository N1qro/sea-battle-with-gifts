import styled from "styled-components";


const StyledFooter = styled.footer`
    display: flex;
    justify-content: space-between;
    padding: 18px 40px;

    border-radius: 1rem 1rem 0 0;
    background: rgba(var(--blue-color-1), 0.50);

    margin-top: auto;
`

export default StyledFooter